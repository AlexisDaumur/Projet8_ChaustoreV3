{% extends 'base.html.twig' %}

{% block body %}
  <div class="container mt-4">
    <h2>Edit my Stock</h2>
    {{ form_start(form) }}
     {{ form_widget(form) }}
     <button class="btn btn-primary">Edit</button>
    {{ form_end(form) }}

  </div>
{% endblock %}
